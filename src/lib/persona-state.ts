export interface PersonaState {
  id: string;
  studentId: string;
  personaId: 'ananya' | 'brinda' | 'chetna';
  completedSemester: number;
  breakMode: boolean;
  stage: string;
  seedVersion: string;
  seedComment: string;
  seededAt: string;
}
